package com.example.javafx_test.opc_ua;

public class BrowseVar {
    /*
    AddressSpace addressSpace = client.getAddressSpace();
        System.out.println(addressSpace);
        //tenho o parent node, então tenho de procurar pela tree pelas minhas variaveis
        NodeId parentNodeId =  new NodeId(4, "|var|CODESYS Control Win V3 x64.Application.GVL");
        UaNode parentNode = addressSpace.getNode(parentNodeId);

// Browse the child nodes
        List<? extends UaNode> childNodes = addressSpace.browseNodes(parentNode);

// Print the child nodes
        for (UaNode childNode : childNodes) {
            System.out.println(childNode.getBrowseName().getName());
            System.out.println(childNode.getNodeId());
            System.out.println(childNode.getDisplayName());
            System.out.println(childNode.getDescription());
        }
     */
}
