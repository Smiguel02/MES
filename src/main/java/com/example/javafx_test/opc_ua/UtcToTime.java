package com.example.javafx_test.opc_ua;

public class UtcToTime {/*
DateTime sourceTime = descriptionValue2.getSourceTime();
        System.out.println(sourceTime);
        Instant instant = Instant.ofEpochMilli(sourceTime.getJavaDate().getTime());
        ZoneId zoneId = ZoneId.systemDefault();
        ZonedDateTime localDateTime = ZonedDateTime.ofInstant(instant, zoneId);
        System.out.println("Source timestamp in local time: " + localDateTime);
*/
}
